name: permit
description: A command-line tool to manage and generate permission files for mobile applications.
version: 0.1.0
repository: https://github.com/Milad-Akarie/permit

environment:
  sdk: ^3.10.4

executables:
  permit: permit

dependencies:
  args: ^2.7.0
  xml: ^6.6.1
  path: ^1.9.1
  interact: ^2.2.0
  collection: ^1.19.1
  ansicolor: ^2.0.3
  yaml: ^3.1.3
  yaml_edit: ^2.2.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  coverage: ^1.15.0
